#import "../components/prelude.typ": *

= 内存管理

== 内存布局

PulseOS 区分了内核虚拟地址空间与用户虚拟地址空间，并在编译时根据生成的 `axconfig.toml` 对布局参数进行静态指定。对于物理地址原语，系统在 `memory_addr` 中的 `PhysAddr`、`VirtAddr` 和 `AddrRange` 等结构提供了强类型的静态编译保障，避免了裸指针带来的寻址混淆开销。

=== 内核虚拟地址空间

内核虚拟地址空间配置参数如下：

#align(center)[
#table(
  columns: (1.4fr, 1.8fr, 1.8fr),
  align: (left, left, left),
  stroke: 0.5pt + rgb("#dddddd"),
  fill: (x, y) => if y == 0 { rgb("#eeeeee") } else { none },
  [参数], [LoongArch64 配置值], [RISC-V 64 配置值],
  [物理内存基址 `phys-memory-base`], [`0x8000_0000`], [`0x8000_0000`],
  [物理内存大小 `phys-memory-size`], [`0x4000_0000`], [`0x4000_0000`],
  [内核镜像物理基址 `kernel-base-paddr`], [`0x8000_0000`], [`0x8020_0000`],
  [内核镜像虚拟基址 `kernel-base-vaddr`], [`0xffff_8000_8000_0000`], [`0xffff_ffc0_8020_0000`],
  [内核空间基址 `kernel-aspace-base`], [`0xffff_8000_0000_0000`], [`0xffff_ffc0_0000_0000`],
  [内核空间大小 `kernel-aspace-size`], [`0x0000_7fff_ffff_f000`], [`0x0000_003f_ffff_f000`],
  [物理-虚拟偏移量 `phys-virt-offset`], [`0xffff_8000_0000_0000`], [`0xffff_ffc0_0000_0000`],
)
]

=== 用户虚拟地址空间

用户地址空间的布局参数如下表所示：

#align(center)[
#table(
  columns: (1.2fr, 2.6fr, 2.7fr),
  align: (left, left, left),
  stroke: 0.5pt + rgb("#dddddd"),
  fill: (x, y) => if y == 0 { rgb("#eeeeee") } else { none },
  [区间名称], [地址范围 / 基地址], [说明与特性],
  [内核虚拟地址空间], [
    LA64: `0xffff_8000_0000_0000` \
    RV64: `0xffff_ffc0_0000_0000` \
    至 `0xffff_ffff_ffff_ffff`
  ], [1 GB 物理内存线性映射等],
  [规范地址空洞], [
    `0x0000_003f_ffff_ffff` 至： \
    LA64: `0xffff_8000_0000_0000` \
    RV64: `0xffff_ffc0_0000_0000`
  ], [未使用的硬件规范虚拟地址空洞],
  [保留/未映射区间], [
    `0x0000_0004_0000_0000` 至 \
    `0x0000_003f_ffff_ffff`
  ], [Sv39 理论用户态上限为 `0x0000_003f_ffff_ffff`，PulseOS 实际空间上限限制为 `0x0000_0004_0000_0000`],
  [用户栈], [`0x0000_0004_0000_0000` \ 向下增长], [缺页自动向下拓展，拥有 1 MB 栈警戒区 `stack_guard_gap`],
  [vDSO], [`0x7f00_0000` 附近], [ASLR 随机化基址，包含加速系统调用的用户态代码段],
  [vvar], [vDSO 紧下方，4 个页面 (`0x4000` 字节)], [提供给用户态免陷入读取的时间、常数等内核映射数据],
  [用户堆], [`0x0000_0000_4000_0000` 向上增长], [初始大小 64 KB，最大限制 512 MB],
  [动态链接器/解释器], [`0x0000_0000_0400_0000`], [动态链接器加载基址],
  [应用 ELF 代码段], [`0x0000_0000_0020_0000`], [采用位置无关可执行文件加载的应用代码/数据段],
  [空指针保护区], [`0x0000_0000_0000_0000` \ 至 `0x0000_0000_0000_1000`], [用于捕获和防止空指针解引用的未映射保护页],
)
]

=== 多架构页表翻译与早期映射

PulseOS 支持在不同架构下实现统一的高级虚拟地址空间管理。其内核屏蔽了底层页表布局的具体差异，通过 `page_table_multiarch` 提供了强类型、与架构解耦的级联页表管理机制。
#par(first-line-indent: 0pt)[
页表翻译与架构级特征：
]
- RISC-V 64：采用 Sv39 模式。Sv39 提供了 39 位的虚拟地址空间，分为三级页表，以 4 KB 页为基本物理映射粒度。
- LoongArch 64：在用户态与内核态均采用多级页表寻址。而在引导期间，MMU 未启用时，LoongArch 64 依赖DMW来访问物理内存和外设。PulseOS 在引导期间配置了 `CSR_DMW0`（无缓存模式，用于外设 I/O）与 `CSR_DMW1`（缓存一致模式，用于物理内存）窗口，以支持在分页模式开启前的代码执行和栈环境建立。当 MMU 启用后，内核会切换至高端虚拟地址（`0xffff_8000_...`）运行，此后内核与用户态空间同样通过级联页表进行转换，以实现精细的页级页表防护与隔离。

引导初期的早期页表映射流程：
- LoongArch 64 引导：由于引导时 MMU 还处于半托管状态，内核引入了静态定义的早期页表，在 CPU 初次跳转至 C 语言运行期之前，手动将基本的串口等外设 I/O 寄存器地址与 RAM 基址进行静态恒等映射。
- RISC-V 64 引导：内核在 `_start` 段运行在物理地址，随后通过临时 Sv39 页表对内核映像进行双重映射，并将页表根物理地址写入 `satp` 寄存器。利用 `sfence.vma` 刷新 TLB 并通过指令跳转切换至高端地址空间运行。

== 内存分配

PulseOS 内存分配相关逻辑实现在 `axalloc` 模块中，采用了两级分配器架构和物理页帧引用计数表配合的分配机制。

#align(center)[
  #image("../img/3.png", width: 58%)
]

=== 动态内存分配器选型与内存开销优化

在系统开发初期，PulseOS 采用了默认的Slab与Buddy分配器。后续开发中为了优化内存占用与提升系统可靠性，我们进行了性能量化分析后，选择替换为TLSF分配器。
#par(first-line-indent: 0pt)[
采集数据如下：
]

#align(center)[
#table(
  columns: (2.2fr, 1.2fr, 1.2fr, 1.4fr),
  align: (left, center, center, center),
  stroke: 0.5pt + rgb("#dddddd"),
  fill: (x, y) => if y == 0 { rgb("#eeeeee") } else { none },
  [评估维度 / 测试项], [Slab+Buddy], [TLSF], [内存与性能优化],
  [内存常驻开销 `used_bytes`], [11.24 MB], [7.72 MB], [-31.3%，内存节省],
  [LoongArch64 Churn 吞吐], [1242.50 K/s], [1417.62 K/s], [+14.1%，吞吐提升],
  [LoongArch64 Realloc 吞吐], [390.05 K/s], [473.17 K/s], [+21.3%，吞吐提升],
  [RISC-V 64 Small Block 耗时], [0.01228 s], [0.01127 s], [-8.2%，耗时缩减],
  [lmbench: 进程 fork+exit 延迟], [4.33 ms], [3.28 ms], [TLSF 领先 32.0%],
  [lmbench: 进程 fork+execve 延迟], [4.03 ms], [3.50 ms], [TLSF 领先 15.0%],
)
]

两级分离检索的 Tuned TLSF 规避了 Slab 分配器的空闲链表检索，在原地扩容、进程创建、映像替换以及缺页异常处理中降低了延迟，加速了内核执行路径。

== 虚拟内存机制

=== 核心组件关系
#par(first-line-indent: 0pt)[
虚拟地址空间的组织结构由以下三个核心机制层层递进构成：
]
- `AddrSpace`：包含了虚拟地址范围，已注册的虚拟内存区域集合，以及锁保护的底层架构相关硬件页表实例。
- `MemorySet`：通用的虚拟内存区间管理容器。它维护虚拟内存区间起始地址到 `MemoryArea` 的 `BTreeMap` 映射，处理地址区间的映射、解映射（涉及区间的裁剪与拆分）以及区间权限保护属性的更新。
- `MemoryArea`：表示一段虚拟地址空间内连续且权限一致的区间范围，解耦了具体的物理页框分配，通过绑定映射后端控制访存行为。

=== 映射后端类型
#par(first-line-indent: 0pt)[
PulseOS 使用多种后端来控制虚拟内存到物理内存的映射动作：
]

#align(center)[
#table(
  columns: (1.5fr, 3.5fr),
  align: (left, left),
  stroke: 0.5pt + rgb("#dddddd"),
  fill: (x, y) => if y == 0 { rgb("#eeeeee") } else { none },
  [后端类型], [描述与实现机制],
  [`Linear`], [线性直接映射：在映射建立时一次性写入页表，不引发缺页异常。常用于内核关键数据段和外设 I/O 空间。],
  [`Alloc`], [动态内存分配：用于用户堆、栈及匿名映射。若不立刻分配物理内存，则采用懒加载通过缺页异常分配并清零物理页。],
  [`File`], [文件内存映射：通过 `CachedFile` 读写文件缓存。支持多进程共享页缓存和私有写时复制副本。],
  [`Cow`], [写时复制包裹：包裹任意只读/懒加载后端，用于在写入时触发写时复制机制。],
  [`Shared`], [共享内存：由 `Arc<SharedFrame>` 控制生命周期的物理连续页，多进程 `shmat` 映射时指向同一组物理地址。]
)
]

==== 动态内存后端 Alloc
#par(first-line-indent: 0pt)[
`Alloc` 后端支持通过 `populate` 参数调节物理内存分配时机：
]
- 立即分配 (`populate = true`)：建立映射时立即填充物理页表，消除后续缺页开销。
- 延迟装载 (`populate = false`，默认)：初次访存时通过缺页异常按需装载。
  1. 若虚拟页完全未映射或为空占位符，调用 `alloc_frame(true)` 分配并清零。
  2. 访问未映射页时，内核会主动探测并一并映射后续 3 个连续未映射页，减少内核态陷入开销，显著提升 `iozone` 顺序读写等测试的性能。

#h(2em)
如果页面已被映射，但发生了权限升级请求（例如在只读页上发生了写访问），系统仅将当前拥有的物理页框附加新标志位，并调用 `pt_guard.remap` 执行原物理页的就地标志位升级，提高了非共享页修改权限的执行效率。

PulseOS还实现了用户栈自动拓展。当用户栈向下增长时，对于带有 `grows_down` 属性的 Alloc 后端，内核会在缺页异常中自动为其向下分配和映射新物理页，并设置了 256 页的 `stack_guard_gap` 警戒区以防止越界覆盖。

==== 文件映射后端 File
#par(first-line-indent: 0pt)[
*共享文件映射（`shared` 属性为 `true`）：*
]
从 `CachedFile` 的全局页缓存中查询或分配物理页地址,然后将其直接映射到页表。由于底层物理页即为页缓存，对该虚拟内存的写入将直接反映在全局页缓存上。在unmap卸载时或通过 `msync` 等调用进行回写同步时，内核会将修改过的页面在 `CachedFile` 中标记为脏页，并调用 `sync()` 保证脏数据落盘。
#par(first-line-indent: 0pt)[
*私有文件映射（`shared` 属性为 `false`）：*
]
初次读取/执行时，通过零拷贝机制直接映射全局 Page Cache 物理页，并清除 `WRITE` 属性，直至发生写访问时，再拷贝至通过 `alloc_frame` 新分配的进程私有物理页，以此保障私有修改不污染全局文件缓存。

==== 写时复制后端 CowMapping

当且仅当发生的缺页异常带有 `WRITE` 属性且页表项中缺乏 `WRITE` 时触发该后端。

内核首先通过 `frame_table().get_ref(old_frame)` 获取该物理页当前的被共享次数，若该物理页不属于 `frame_table` 追踪范围，则默认共享数大于 1 以保守处理。

在引用计数为1时，说明已无其他进程共享该物理页，此时无须执行物理内存分配和数据复制，而是直接调用 `pt_guard.remap` 将当前页升级为可读写，从而提升独占 COW 页的写入效率。

若引用计数大于1，则分配新的物理页框并将原页内容完整复制到新页中，更新页表指向新页框并开启写权限，随后调用 `dealloc_frame(old_frame)` 递减旧页框的引用计数。若属首次访问，写时复制后端会委托内部包裹的文件或匿名分配后端执行初始分配映射。

=== 地址空间克隆与页表复制
在 `fork` 调用中，`AddrSpace` 的克隆与页表复制并不是在高级层逐页进行的，而是通过修改的`page_table_multiarch`提供的递归克隆接口 `copy_cow_range` 高效实现：
- `copy_cow_range`会自上而下递归遍历源页表，当目标页表未分配对应的中间级页表项时，会自动调用 `alloc_table` 分配并补全，动态复制出与源页表完全相同的层级路径，避免了高级层频繁陷入与映射的开销。
- 如果 `is_cow` 参数为 `true`，接口会检查源页表项。若源 PTE 可写，则清除写属性使其降级为只读，并在目标页表中以只读权限映射相同的物理页帧，使父子进程安全地共享同一块物理内存。
- 在映射物理帧时，通过传入的 `inc_ref` 回调闭包（即 `cow_inc_frame_ref`）增加物理页帧在全局 `FrameTable` 中的引用计数。当后续任何一方尝试写入该只读页面时，即可基于该引用计数触发COW缺页处理。
以 `fork` 为例，PulseOS 依赖 `try_clone` 克隆地址空间。在克隆过程中，各 MemoryArea 后端对底层的页表映射进行共享和克隆，构建 COW 关系：
#align(center)[
  #image("../img/4.png", width: 110%)
]

== 内存锁定支持

PulseOS 实现了 POSIX 兼容的内存锁定相关功能，如 `mlock`, `mlockall` 等系统调用。对于页面仅修改了虚拟元数据而未装载物理帧时会主动触发缺页处理，强制分配映射对应物理页，以保障实时任务的高优先级物理内存供给。