#import "../components/prelude.typ": *

= 进程管理

== 数据结构

PulseOS 的进程/线程设计采用双层结构，实现了资源分配与调度执行的解耦。其核心实体包括 `Process`（进程控制块）、`Thread`（线程控制块）与 `TaskInner`（ArceOS 原生任务单元），三者的关联关系如下所示：

#align(center)[
  #image("../img/5.png", width: 95%)
]

- `TaskInner`：ArceOS 内核感知的底层任务执行单元。负责保存任务运行时的硬件上下文及生命周期状态（`Running`、`Ready`、`Blocked`、`Exited`），并通过任务扩展机制绑定上层的 `Thread`。
- `Thread`：任务调度的基本单元。保存线程特有的信号掩码、调度策略、`clear_child_tid` 等状态信息。为了避免与 `TaskInner` 形成双向强引用环，`Thread` 内部通过弱引用指向 `TaskInner`，同时亦以弱引用关联其所属的 `Process`。
- `Process`：资源分配的最小单元。管理文件描述符表、用户虚拟地址空间以及该进程所属的所有线程集合等全局资源。

== 任务调度

=== 特殊任务

#par(first-line-indent: 0pt)[
PulseOS 在每个 CPU 核心初始化时会建立独立的运行队列并添加以下任务：
]

- main 任务：初始化时会向主 CPU 的运行队列中加入该任务，用于启动内核。
- idle 任务：当就绪队列为空时，PulseOS 就会进入该任务循环，直至有新任务进入就绪状态。
- gc 任务：对已退出的任务，调用 `exit_current` 时将其加入 `EXITED_TASKS` 队列并唤醒 `gc` 任务，`gc` 会立即释放内核栈空间，防止栈内存耗尽。

=== 调度器策略与接口定义

PulseOS 中的调度策略接口被抽象为 `BaseScheduler` Trait，使用编译时 feature 指定调度策略，提供了以下接口：
- `add_task` / `remove_task`：添加或移除可运行任务。
- `pick_next_task`：选择并弹出下一个需要执行的任务。
- `put_prev_task`：将前一个运行的任务放回就绪队列，根据 `preempt` 参数决定是插在队头抢占位置还是队尾时间片耗尽与主动让出位置。
- `task_tick`：由定时器中断定期触发，推进当前任务的时间片并返回是否需要重新调度。

#h(2em)
同时PulseOS在ArceOS提供的 `RRScheduler` 的基础上实现了优先级机制，并以此管理就绪任务。

我们在就绪队列内部定义了 99 个实时 RT 优先级队列与 1 个普通优先级队列。任务通过 `priority` 和 `time_slice` 分别管理优先级和分配的时间片。

为了避免在高并发多任务下 $O(N)$ 遍历就绪队列，我们还在 RRScheduler 中引入了 `rt_bitmap: u128` 位图。每一位对应一个 RT 优先级队列，当某个队列中有任务就绪时，位图中对应的位置为 1。调度器通过 `trailing_zeros()` 等二进制搜索函数，可以在 $O(1)$ 时间内快速定位当前具有就绪任务的最高优先级队列。

=== 定时器与时间片更新
PulseOS 将定时器中断钩子注册到 `axtask` 中，其整体驱动流程如下：

#align(center)[
  #image("../img/6.png", width: 104%)
]

#h(2em)
在中断/异常处理完毕准备返回用户态的边界处，系统会自动检测当前任务的抢占挂起标志 `preempt_pending`。若该标志被置为 `true`（如当前任务时间片耗尽或被高优先级任务抢占），则触发 `preempt_resched()` 执行上下文切换，完成抢占调度流程。

而在上述流程的 `scheduler.task_tick()` 步骤中，对于采用时间片轮转的就绪任务，其时间片消耗与抢占判定的具体逻辑如下：

#align(center)[
  #image("../img/7.png", width: 86%)
]

*间隔定时器 itimer 机制*

不同于基于时钟中断周期性 Tick 递减的虚拟/配置文件定时器（`itimer_virt`、`itimer_prof`），真实时间定时器 `itimer_real` 采用了异步的高精度管理与 `SIGALRM` 信号触发机制：
- 异步高精度触发：不同于基于 Tick 轮询递减的虚拟定时器 `itimer_virt` 与配置文件定时器 `itimer_prof`，`itimer_real` 采用高精度异步触发机制。用户设置定时器时，系统直接调用 `axtask::set_generic_timer` 向底层的时钟中断事件管理器注册事件回调，免去了在 Tick 钩子中进行全局扫描的开销。当时钟中断到达设定截止时间时，回调被精确触发并向进程发送 `SIGALRM` 信号。若设定了周期性时间（`interval > 0`），系统会自动计算下一次触发的 `deadline` 并递归重新注册，有效降低了每个 Tick 下的全局扫描与 CPU 遍历开销。
- 惰性唤醒失效机制（Timer Ticket ID）：为了防止已被非超时事件提前唤醒并激活的线程再次被后续的超时事件错误触发，PulseOS在定时器注册时引入了唯一的 `ticket_id` 凭证。当线程因非超时事件提前唤醒时，其持有的 Ticket 会被标记为过期。当时钟中断回调触发时，系统会校验注册时的 Ticket ID 与当前线程状态的一致性，若检测到状态不一致，则直接丢弃该唤醒事件，保障了超时唤醒的准确性与安全性。

=== 异常处理与硬中断流程

PulseOS 针对多架构，即 RISC-V 64 与 LoongArch 64，构建了高度一致的硬异常与中断处理架构。
#par(first-line-indent: 0pt)[
硬件陷阱 Trap 发生与跳转：
]
  - RISC-V 64：当硬件产生异常或中断时，硬件会将当前 CPU 特权级自动提升到 Supervisor 模式，将中断前的指令指针存入 `sepc` 寄存器，引发原因存入 `scause`，附加信息，如非法访存地址，存入 `stval`，并将特权状态映射到 `sstatus` 中。随后，CPU 跳转到由 `stvec` 寄存器指示的内核 Trap 向量入口 `trap_vector` 处。
  - LoongArch 64：触发异常时，CPU 同样进入内核特权模式，将断点 PC 保存至 `CSR_ERA`，将异常类型写入 `CSR_ESTAT`，原特权状态写入 `CSR_PRMD`，并在包含映射错误时将非法地址填入 `CSR_BADV`。CPU 随后根据 `CSR_EENTRY` 指示的异常入口基址进行硬件跳转。
通用陷阱上下文 TrapFrame 保存与恢复：

  在进入 trap 入口后，内核通过汇编代码首先保存当前任务的通用寄存器，即 RISC-V 的 `x1` 至 `x31`，LoongArch 的 `r1` 至 `r31`，到该线程的内核栈底部的 `TrapFrame` 结构中。为了支持多架构下的用户态切换与 `fork` 系统调用，在发生陷阱时必须正确恢复内核栈指针并对 `kernel_sp` 进行保护：
  - RISC-V 64：在进入用户态前，将内核栈顶地址写入 `sscratch` 寄存器。陷入时，CPU 执行 `csrrw sp, sscratch, sp` 交换寄存器，从而恢复内核栈指针并把用户栈指针保存在 `sscratch` 中。
  - LoongArch 64：利用自定义的 `KSAVE_KSP` 寄存器（映射到 `CSR_KSAVE0`，编号 `0x30`）来保护内核栈上下文。进入用户态前调用 `write_kernel_sp` 更新该寄存器；陷入时，汇编入口通过 `csrrd $sp, KSAVE_KSP` 恢复内核栈指针。
  随后，内核将当前栈指针寄存器 `sp` 重定向为指向 `TrapFrame`，并调用 `trap_handler` 高级 Rust 分发入口。当分发执行完毕时，通过相反路径从栈底 `TrapFrame` 中恢复所有寄存器状态，最后执行物理返回指令，即 Supervisor 返回与 Exception 返回，重置特权级并返回断点。

=== 系统调用处理与安全屏障

PulseOS 兼容了 Linux POSIX 系统调用规范，提供了 ABI 级别的二进制兼容。

#par(first-line-indent: 0pt)[
系统调用入口与 ABI 参数传递：
]

- 用户程序执行陷入指令，即 RISC-V 64 的 `ecall` 与 LoongArch 64 的 `syscall 0`，触发系统调用陷阱。
- 系统调用的 ABI 规范强制参数通过寄存器传递。参数 `syscall_id` 在 RISC-V 中由 `a7` 寄存器传入，在 LoongArch 中由 `a7` 传入；具体的系统调用参数在两架构中均统一由 `a0` 至 `a5` 寄存器传入。
- 系统调用返回后，错误码或返回值均回填到寄存器 `a0` 中。为了符合 Linux ABI 的兼容性设计，若执行失败，返回的错误码均取负数，如 `-EFAULT` 与 `-EINTR`，由 C 库，即 musl-libc 或 glibc，进行拦截并重填用户态的全局 `errno` 变量。
- Syscall 自动递增 PC ：为了支持系统调用执行完毕返回用户态后能继续执行下一条指令，在处理系统调用异常（RISC-V 的 `UserEnvCall` 与 LoongArch 的 `Syscall` 异常）时，内核会首先将异常现场的程序计数器（SEPC / ERA）自动递增 4（即 `tf.sepc += 4` 与 `tf.era += 4`），然后再分发给具体的系统调用处理函数，以避免在返回用户态时重复触发陷入指令而陷入死循环。

  为了实现物理的内核-用户态隔离，内核对用户传入的所有地址均执行严格的校验与受限拷贝：
  - `validate_user_range` 边界验证：在读写任何用户内存，如路径字符串、读写缓冲区、信号上下文等之前，内核对传入指针区间进行严格的加法溢出检查，确保其起始地址与长度完全落在 `USER_SPACE_BASE` 至 `USER_SPACE_BASE + USER_SPACE_SIZE`之间，任何非法的越界访问均被拦截并返回 `-EFAULT`。
  - 数据拷贝安全保护：PulseOS 通过 `read_user_cstring_bytes` 与 `read_user_plain_array` 等接口提供数据拷贝屏障。在读取用户空间数据，例如文件路径与缓冲区内容，时，内核通过受保护的安全机制访问当前进程 `AddrSpace` 对应的页表与物理页面，以受控方式安全地拷贝数据，保护内核不受非法用户态地址指针的影响。

=== 上下文切换与寄存器保存机制

#par(first-line-indent: 0pt)[
作为 PulseOS 的上下文切换机制由 ArceOS 提供，其细节设计如下：
]

- 运行队列与 `on_cpu` 跨核死锁预防：
  每个 CPU 核心拥有独立的运行队列 `AxRunQueue`。在多核 SMP 环境下，当 CPU A 调度切出任务 A 时，汇编代码将当前 CPU A 的硬件上下文保存到任务 A 的栈中。为了防止任务 A 在 CPU A 尚未完全完成切换时被 CPU B 瞬间唤醒并在 CPU B 的物理寄存器上运行，因为这会由于两核同时写入同一个内核栈导致严重的栈损坏，内核为每个任务引入了 `on_cpu` 原子状态标记。在 CPU A 彻底完成 switch 汇编跳转前，CPU B 如果尝试唤醒该任务，必须在 `clear_prev_task_on_cpu` 自旋等待该标记清空，从而杜绝了跨核并发栈损坏。
- 上下文保存汇编：底层利用汇编代码切换 CPU 的被调用者保存寄存器，其中 RISC-V 64 为 `s0` 至 `s11` 与返回指针 `ra`，LoongArch 64 为 `s0` 至 `s8`、`fp` 与返回指针 `ra`。
- 特权任务指针检索与 TLS：
  在 RISC-V 64 和 LoongArch 64 架构下，任务指针通过线程局部存储 TLS 的偏移量来进行硬件寻址。
- 惰性浮点寄存器切换：
  为避免频繁的浮点寄存器切换造成内存总线开销，系统在 RISC-V 64 架构下实现了惰性保存策略：只有当当前任务触发过浮点运算，导致特权寄存器 `sstatus.FS` 处于 `Dirty` 状态时，在切出时才保存浮点寄存器组；否则直接跳过，显著节省了纯整型计算线程的切换延迟。而 LoongArch 64 在配置 `fp-simd` 时，则采取了每次主动保存机制。

== 生命周期管理

=== 进程与线程的创建

系统通过 sys_clone 与 sys_clone3 系统调用实现进程与线程的创建，支持线程与进程级别的资源克隆，具体创建流程如下：

#align(center)[
  #image("../img/8.png", width: 70%)
]

=== `vfork` 挂起同步机制

当使用 `CLONE_VFORK` 创建子进程时，父进程与子进程共享相同的用户虚拟内存空间，且父进程必须被挂起，直到子进程释放该地址空间。
- 同步原语：父进程在克隆完子进程后，在 `vfork_context.event` 等待队列上阻塞。
- 解除挂起：子进程在成功替换页表后，或者在进程退出的时候，会主动调用 `complete_vfork()` 唤醒父进程。

=== 进程映像替换 execve

进程映像替换由 `exec` 实现，其设计遵循半事务性的安全原则：

1. *预加载与验证*：在释放当前地址空间之前，先解析脚本解释器路径，检测是否有其他进程正在写入该文件，防止 `ETXTBSY`，校验执行权限，并对 ELF 头部格式进行初步校验。此阶段若出错，可直接向用户态安全返回错误码，而不破坏原进程。
2. *空间切换与原空间释放*：分配一个全新的隔离地址空间 `new_aspace` 并映射初始的栈与堆。在此之后，系统执行不可逆的切换操作——将进程的地址空间句柄替换为 `new_aspace`，激活并切换当前 CPU 页表根节点，随后立即释放原有的地址空间 `old_aspace` 以降低内存峰值。
3. *程序载入与单向终止*：在激活新页表后，才真正开始将新程序段及动态链接器载入内存。由于此时原地址空间已被完全销毁释放，若在载入阶段发生 ELF 损坏或其它加载期错误，原进程状态无法恢复，系统会在内核态直接终止当前进程，而不会返回错误到用户态。
4. *清理旧状态*：加载成功后，断开所有原共享内存关联，清空信号量 undo 队列，关闭设置了 `FD_CLOEXEC` 标志的文件描述符，并重置所有非忽略信号的行为为默认动作。

=== 进程退出与资源回收

进程生命周期的终点是退出，其拆卸和回收过程分为两个阶段：

1. 阶段一：用户态资源同步回收
当进程的最后一个线程执行退出逻辑时，由当前线程在内核态同步完成大部分用户空间资源的清理：
- *状态标记与同步唤醒*：进程被标记为僵尸进程，并调用 `complete_vfork()` 唤醒因 `CLONE_VFORK` 阻塞等待的父进程。
- *切换全局僵尸页表*：为了避免当前运行的核心在彻底销毁进程页表时发生非法的内存地址访问，系统使用全局唯一的空虚拟地址空间页表 `ZOMBIE_ASPACE` 替换并激活当前物理页表，随后立即释放并回收原进程的地址空间。
- *释放共享资源*：同步卸载并断开所有共享内存关联，清除信号量 undo 队列，关闭进程中所有打开的文件描述符，清理 Futex 等待队列，释放所有内存锁定，并重置文件系统上下文、精简用户凭证及进程执行参数。

2. 阶段二：内核态资源异步回收
在线程完成第一阶段清理并触发 CPU 调度切换后，进入内核资源的最终回收：
- *异步 GC 回收内核栈*：已退出的线程实例被挂载到该 CPU 的全局 `EXITED_TASKS` 队列中。CPU 专属的异步垃圾回收协程 `gc_entry` 会被唤醒，从队列中取出任务并调用 `task.reclaim_kernel_stack()` 释放该任务占用的庞大内核栈，防止发生内存溢出。
- *僵尸进程收割与完全销毁*：父进程（或在父进程先死时托管的 `init` 进程）通过 `wait4` / `waitpid` 系统调用，将子进程从其 `children` 列表中移除。当子进程 PCB 的所有强引用计数归零时，PCB 结构体及剩余的内核管理对象被彻底销毁。
