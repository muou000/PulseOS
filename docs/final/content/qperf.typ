= qperf 改造与分析

== 改造目标
BuildStorm 持续时间长、并发任务多，对内核的内存、文件系统等各个模块都存在要求。对于原版qperf，既没有SMP采样分析的能力，也无法把 futex、块设备等待和调度迁移等阻塞事件与任务对应，面对buildstorm这种复杂测试已经不足以使用了。因此我们对 qperf 进行了以下两部分改造：

+ 修改内核导出 ABI `__pulse_qperf_trace_v1`。QEMU会匹配 ELF 解析其地址并传给 QEMU plugin，plugin 在该地址读取 ABI 参数并还原 trace 事件。
+ 在任务、调度、运行队列和等待路径记录任务元数据、调度切换、阻塞、唤醒、入队和退出事件。入队事件保留 spawn、wake、preempt、yield、迁移、deferred wake 和在启用对应路径时的 work-steal 原因；阻塞事件保留 wait reason、资源标识和唤醒来源。

该设计把工作负载阶段、CPU 路径与调度事件关联起来，用于定位问题；它只承担机制分析，不改变普通内核的正式构建结果。

== 分析结论

改造后的qperf对PulseOS的性能优化存在以下几点价值：
+ 我们可以通过采样结果找出下一步优化的方向，无论是on-cpu的性能问题，还是off-cpu的阻塞问题。
+ 我们还可以通过qperf进行修改前后的采样，以此来证明修改前后是否有效，目标路径开销是否降低。
