= AI 使用说明

== 使用定位

在决赛线上阶段，AI 的主要用途是分析 qperf 采样、测试日志和相关源码路径，梳理可能的性能热点、阻塞关系与后续排查方向，以及选择或编写合适的测试对修改前后的效果进行验证与测试。同时，AI也用于对部分性能热点路径进行修改优化。

== 使用方式

每次协作开始前，我们都会先提供明确的任务范围，例如相关源码路径、复现步骤、构建或运行日志、采样产物和预期行为。AI 的输出均视为待核查的建议，而不直接采纳，主要包括以下几个方面：

+ 将日志、调用栈和采样结果关联到候选代码路径；
+ 对比修改前后的实现，提出兼容性、并发性等风险；
+ 协助拆分修改步骤，使结果更易于人工检查；
+ 协助整理测试记录、实验说明和设计文档。

对于涉及内存管理、调度、文件系统、网络和系统调用等内核关键路径的建议，我们会结合实际调用链以及qperf采样数据分析进行复核。

== 验证与证据边界

AI 可以帮助提出性能假设，但不能据此直接证明优化有效。qperf 专用内核提供的是 on-CPU 采样、调度与阻塞事件等机制层证据，用于定位候选热点。

性能结论须由人工复核后的源码分析与受控实验共同支持。对比测试需要固定内核与 ELF、插件与分析器、镜像、QEMU 参数及工作负载输入，并检查构建日志、运行日志和采样产物的完整性。普通内核的实际工作负载结果与 qperf 机制分析应分别记录，避免将 AI 推断、采样线索和可复现实验结果混为一谈。

== AI开发案例

以 qperf 的使用为例，我们会在完成采样后先交给 AI 做初步数据处理，并输出分析报告。随后结合上面的数据报告和实际调用链人工复核，再让 AI 协助给出修改方案和测试计划。方案确认后执行修改与测试，最后继续结合 AI 的分析结果验证优化是否有效。