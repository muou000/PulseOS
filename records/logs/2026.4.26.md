完善增强了文件系统缓存机制，但是仍存在部分问题(部分ai prompt可在PR中的评论找到)

修复了COW存在的部分bug，同时修复了Process/Thread存在的问题

使用AI修复了loongarch64在测评容器中卡死在启动阶段的问题(codex GPT-5.5)

```Markdown
Prompt:
请直接修改 PulseOS 项目，排查并修复 loongarch64 在测评环境中使用 run_test.sh 启动 QEMU 时卡死无输出的问题。修改完成后，只需要最后给我报告。

已确认：

- 本机可以正常运行；
- 测评环境 QEMU 无任何串口输出，30s 超时退出；
- 本机和测评环境的 QEMU 启动参数一致；
- 本机和测评环境使用的 kernel-la 确定是同一个文件；
- loongarch64 下 ArceOS 层没有进入，没有 ArceOS Logo；
- testcode_cmd.sh 已设置为进入环境、挂载测评镜像后立即退出；
- 测评脚本不能修改，只能修改 PulseOS 项目；
- 优先排查 axplat-loongarch64。

请不要继续排查 QEMU 参数差异或 kernel-la 文件差异，这些已经排除。

请重点检查并修改：

- axplat-loongarch64 的汇编入口；
- boot stack 初始化；
- BSS 清零；
- linker script；
- kernel entry / load address / virtual address；
- exception vector；
- MMU / 页表映射；
- UART MMIO early output；
- FDT 解析；
- Rust 入口跳转；
- 是否在串口初始化前卡死。

请直接添加 loongarch64 early debug 输出。普通串口驱动不可用时，直接通过 QEMU virt 的 UART MMIO 地址输出单字符标记。至少区分：

- 是否进入汇编入口；
- 是否完成栈初始化；
- 是否完成 BSS 清零；
- 是否完成页表初始化；
- 是否成功开启 MMU；
- 是否进入 Rust 入口；
- 是否进入 ArceOS 初始化。

最终报告请包含：

1. 问题定位结论；
2. 修改过的文件和关键代码位置；
3. 新增 early debug 标记含义；
4. 为什么这些修改能适配测评环境；
5. 如何查看 tmp.txt 和 PulseOS/os_serial_out_la.txt 验证；
6. 剩余风险和下一步定位方向。
```

具体修改内容可见[commit](https://github.com/muou000/PulseOS/commit/3b00ed2a2d44d447395d395f45513c575e675b8f)