#### 5月25日

实现了/dev/null,实现了/proc/cmdline等procfs功能

#### 5月26日

优化了管道的阻塞等待机制，改造了Stdin的轮询机制，使用缓冲区异步轮询获取输入内容

使用AI修改了axtask_ext相关代码，但是考虑到可能带来的性能影响，进行了回退

将pulse_syscalls中对fd_table等的操作封装进了pulse_core中，让两者职责分立更清晰

同时使用vecDeque替换了BtreeMap作为fd_table的结构实现，后续可能会向Linux的实现靠拢

实现了/proc/[pid]/stat中的部分字段

以成功运行iperf为目标，由ai完成了对应的修复

修复了vendor下.txt文件被过滤掉的问题

#### 5月27日

修改了execve,使其先检验权限与文件类型再执行

以ltp运行日志为反馈，测例代码为资料，部分测例的通过由ai完成

实现了空洞文件与按需块分配

修复了信号机制，使某个线程触发进程退出时能使其他所有线程都完成退出，避免造成阻塞

#### 5月28日/5月29日

修改完善了时间系统，mmap，fork，execve/execveat等，使其相关ltp 测例可以顺利通过（绝大部分），这一部分大量使用了ai，具体流程如下：
1. 输入格式为结合 [测例代码] 和 [ltp运行日志] ，制订修改计划，使PulseOS可以尽可能多的顺利通过这些测例
2. review ai提供的修改计划，然后ai进行修改，修改完成后又我自行进行测试检查
3. 网络相关测例由于我的了解程度并不高，没办法有效review ai提供的修改方案，只能全盘accept。而且现有网络功能也是由ai完成的，所以网络模块是目前PulseOS最大的缺陷点。